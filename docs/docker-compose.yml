services:
  docs:
    container_name: 'ph-address-intel-docs'
    build:
      context: .
      dockerfile: docs/Dockerfile
    ports:
      - "8091:80"
    restart: unless-stopped
    networks:
      - ph_address_intel_net
      - proxy_manager_net

networks:
  ph_address_intel_net: 
  proxy_manager_net:
    external: true

